#include "main.h"


int main() {
	
	hideC;
	fcolor(150,150,150);
	bcolor(10, 25, 47);
	body();
	footer();
	loginpage();
	
	
	//move(16, centerN(emailbarL) + 1);
	//clearL(emailbarL - domain.length() - 2);
}